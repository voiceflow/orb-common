parameters:
  commit_message:
    description: Commit message to check
    type: string
steps:
  - run:
      name: Check commit message
      environment:
        CHECK_COMMIT_MESSAGE: << parameters.commit_message >>
      command: <<include(scripts/utils/check-commit-message.sh)>>
