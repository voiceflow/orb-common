parameters:
  step_name:
    description: Name of the step
    type: string
    default: Set etc hosts
  run_in_background:
    description: run the command in background
    type: boolean
    default: false
steps:
  - run:
      name: << parameters.step_name >>
      background: << parameters.run_in_background >>
      command: <<include(scripts/utils/set-hosts.sh)>>
