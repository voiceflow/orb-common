parameters:
  executor:
    description: Executor to run the command on
    type: executor
    default: node-executor-node-20
  command:
    description: Script to execute tests
    type: string
    default: yarn build --summarize true && yarn test:unit
executor: << parameters.executor >>
steps:
  - checkout_clone
  - attach_workspace:
      at: ~/voiceflow
  - turborepo_install
  - turborepo_generate_hashfile:
      prefix: build-cache
      command: yarn build
  - turborepo_restore_cache:
      prefix: build-cache
  - run:
      command: << parameters.command >>
  - turborepo_store_summaries
  - turborepo_store_test_results:
      report_name: ./unit.report.xml
  - turborepo_persist_coverage:
      coverage_dir: ./sonar/unit-coverage
