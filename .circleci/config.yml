version: 2.1
orbs:
  orb-tools: circleci/orb-tools@2.0.0

workflows:
  publish-orb:
    jobs:
      - orb-tools/increment:
          orb-path: orb.yml
          orb-ref: voiceflow/common
          publish-token-variable: ${CIRCLE_TOKEN}
          segment: patch
          validate: true